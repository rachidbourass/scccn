<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE Workflow PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<Workflow explicitTransitions="true" name="Workflow-Ascension-ReleaseExcludedLeaver" type="">
  <Variable input="true" name="trigger">
    <Description>The IdentityTrigger</Description>
  </Variable>
  <Variable input="true" name="event" transient="true">
    <Description>The IdentityChangeEvent.  It can be used to build
      the provisioning plan, but does not need to be
      persisted with the case, so marked as transient.</Description>
  </Variable>
  <Variable input="true" name="identityName">
    <Description>The name of the identity.</Description>
  </Variable>
  <Variable initializer="false" name="trace">
    <Description>Used for debugging this workflow and when set to true trace
      will be sent to stdout.</Description>
  </Variable>
  <Variable initializer="true" name="transient"/>
  <Variable name="launcher"/>
  <Variable name="inputIdentity"/>
  <Step icon="Start" name="Start" posX="20" posY="20">
    <Transition to="Required Input Form"/>
  </Step>
  <Step icon="Stop" name="Stop" posX="1050" posY="32"/>
  <Step icon="Provision" name="Required Input Form" posX="446" posY="26">
    <Approval name="Release Excluded Leaver" owner="ref:launcher" return="inputIdentity" send="">
      <Form name="Release Excluded Leaver">
        <Attributes>
          <Map>
            <entry key="hideIncompleteFields">
              <value>
                <Boolean/>
              </value>
            </entry>
            <entry key="includeHiddenFields">
              <value>
                <Boolean/>
              </value>
            </entry>
            <entry key="pageTitle" value="Release Excluded Leaver"/>
          </Map>
        </Attributes>
        <Section name="Section 1">
          <Field displayName="Identity" filterString="leaverDate == &quot;19000103&quot;" helpKey="Select Excluded Leaver" name="inputIdentity" required="true" type="sailpoint.object.Identity"/>
        </Section>
        <Button action="next" label="Clear leaverDate"/>
        <Button action="cancel" label="Nevermind"/>
      </Form>
    </Approval>
    <Transition to="Clear leaverDate"/>
  </Step>
  <Step icon="Default" name="Clear leaverDate" posX="729" posY="32">
    <Script>
      <Source><![CDATA[    import sailpoint.object.Identity;

    Identity refIdentity = context.getObjectById(Identity.class, inputIdentity);

    if (refIdentity != null && refIdentity.getAttribute("leaverDate") != null) {
    
    refIdentity.setAttribute("leaverDate","");
    
    context.saveObject(refIdentity);
    context.commitTransaction();

    }]]></Source>
    </Script>
    <Transition to="Stop"/>
  </Step>
</Workflow>