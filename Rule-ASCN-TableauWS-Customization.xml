<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE Rule PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<Rule language="beanshell" name="ASCN-TableauWS-Customization" type="ResourceObjectCustomization">
  <Description>This rule is configured on the application and is called after the connector has build a ResourceObject from the native application data. Initially designed for non-rule based connectors to add SPPrivileged flag to an object, but could be used to do any transformations.</Description>
  <ReferencedRules>
    <Reference class="sailpoint.object.Rule" name="ASCN-RuleLibrary-Common"/>
  </ReferencedRules>
  <Signature returnType="ResourceObject"/>
  <Source><![CDATA[ 
    String logName = "ASCN-TableauWS-Customization";
    trace(logName,"Entering...");

    String siteRole = object.getStringAttribute("siteRole");
    if("Unlicensed".equals(siteRole)){
       object.put("IIQDisabled",true);
    }
    trace(logName,"Exiting...");
    return object;
]]></Source>
</Rule>